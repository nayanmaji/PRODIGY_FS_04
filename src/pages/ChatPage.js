import React from 'react';
import ChatList from '../components/Chat/ChatList';

const ChatPage = () => {
    return (
        <div>
            <ChatList />
        </div>
    );
};

export default ChatPage;
